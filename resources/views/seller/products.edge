<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/img/logo.png" type="image/x-icon" />
    <link rel="stylesheet" href="{{ asset('/css/seller_products.css') }}">  
    <title>{{ title || 'Seller About | RUZÉ Co.' }}</title>
</head>
<body>
    @include('partials/nav')

<section class="seller-products" style="margin-top: 100px;">
  <h1 class="heading"><span>Manage</span> Products</h1>

    <div class="add-product">
        <form action="/seller/add-product" method="POST" class="product-form">
        <input type="text" name="title" placeholder="Product Title" required />
        <input type="text" name="image" placeholder="Image URL" required />
        <input type="number" name="price" placeholder="Price" required />
        <button type="submit" class="btn">Add Product</button>
        </form>
    </div>

    <h2 class="heading"><span> All </span> Products </h2>

    <section class="products">
        <div class="box-container">
        @each(product in products)
            <div class="box">
            <div class="image">
                <img src="{{ product.image }}" alt="{{ product.title }}" />
                <div class="icons">
                </div>
            </div>
            <div class="content">
                <h3>{{ product.title }}</h3>
                <div class="price">Rp {{ product.price }}</div>
                <p style="font-size: 1.2rem; color:#666;">
                Seller: <b>{{ product.sellerEmail }}</b>
                </p>
                @if(product.sellerEmail === user.email)
        <form action="/seller/delete-product" method="POST" style="margin-top:10px;">
        <input type="hidden" name="id" value="{{ product.id }}">
        <button type="submit" class="delete-btn">Delete</button>
        </form>
    @endif
            </div>
            </div>
        @endeach
        </div>
    </section>
    </section>

@include('partials/footer')

</body>
</html>