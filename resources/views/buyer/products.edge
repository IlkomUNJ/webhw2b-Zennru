<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="{{ asset('/css/styles.css') }}">
  <link rel="icon" href="/img/logo.png" type="image/x-icon" />
  <link rel="stylesheet" href="{{ asset('/css/product.css') }}">
  <title>Buyer Products | RUZÉ Co.</title>
</head>
<body>
  {{-- Navbar --}}
  @include('partials/nav')

  <section class="products" id="products" style="margin-top: 120px;">
    <h1 class="heading"><span> Our </span> Products <span> Collection </span></h1>

    <section class="search-bar">
      <form action="">
        <input type="text" placeholder="Cari produk..." id="search-box" />
        <button type="submit">Search</button>
      </form>
    </section>

    <div class="box-container">
      @each(product in products)
        <div class="box">
          <div class="image">
            <img src="{{ product.image }}" alt="{{ product.title }}" />

            <div class="icons">
              <form action="/buyer/add-wishlist" method="POST" class="wishlist-form">
                <input type="hidden" name="productId" value="{{ product.id }}">
                <input type="hidden" name="sellerEmail" value="{{ product.sellerEmail }}">

                @if(wishlistIds.includes(product.id))
                  <button type="submit" class="fas fa-heart big-heart active-heart" title="Remove from wishlist"></button>
                @else
                  <button type="submit" class="fas fa-heart big-heart" title="Add to wishlist"></button>
                @endif
              </form>

              <form action="/buyer/add-cart" method="POST" class="cart-form">
            <input type="hidden" name="productId" value="{{ product.id }}">
            <input type="hidden" name="sellerEmail" value="{{ product.sellerEmail }}">
            <button type="submit" class="cart-btn">ADD TO CART</button>
            </form>
            </div>
          </div>
          <div class="content">
            <h3>{{ product.title }}</h3>
            <div class="price">Rp {{ product.price }}</div>
          </div>
        </div>
      @endeach

      @if(products.length === 0)
        <p style="text-align:center; font-size:1.6rem; color:#666; margin-top:3rem;">
          Belum ada produk yang tersedia saat ini.
        </p>
      @endif
    </div>
  </section>

  {{-- Footer --}}
  @include('partials/footer')
</body>
</html>
